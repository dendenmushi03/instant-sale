<!doctype html>
<html lang="<%= lng || 'ja' %>">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= t('checkout.done_title') %></title>
  <link rel="stylesheet" href="/public/style.css?v=<%= assetVer %>" />
</head>

<body class="layout">

<%- include('partials/header') %>

<% const locale = (lng === 'en' ? 'en-US' : 'ja-JP'); %>

<main class="container purchaseComplete">
  <section class="card success purchaseComplete__card" aria-labelledby="purchaseCompleteTitle">
    <h2 id="purchaseCompleteTitle" class="purchaseComplete__title"><%= t('checkout.done_title') %></h2>
    <div class="purchaseComplete__copy">
      <p class="purchaseComplete__recommend"><%= t('checkout.recommend_note') %></p>
      <p><%= t('checkout.step_open') %></p>
      <p><%= t('checkout.step_save') %></p>
      <p><%= t('checkout.step_expire') %></p>
      <p class="muted purchaseComplete__meta">
        <%- t('checkout.expire_note_html', { minutes: ttlMin, time: expiresAt.toLocaleString(locale) }) %>
      </p>
    </div>

    <p class="purchaseComplete__actions">
      <a class="btn btn--primary" href="<%= saveUrl %>"><%= t('checkout.download_btn') %></a>
    </p>
  </section>
</main>

<%- include('partials/footer') %>

</body>
</html>
